% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runtime_helpers.R
\name{CECsample_diamonds}
\alias{CECsample_diamonds}
\title{Sample a small diamonds data set ready for mixed-data clustering.}
\usage{
CECsample_diamonds(n_max = 100, seed = NULL)
}
\arguments{
\item{n_max}{Maximum number of rows to return.}

\item{seed}{Optional random seed.}
}
\value{
A data frame with four numeric variables and three factor variables.
}
\description{
This helper loads \code{ggplot2::diamonds}, randomly samples at most \code{n_max} rows,
and converts the ordered quality columns to plain factors.
}
\details{
This helper requires the optional package \pkg{ggplot2}.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
CECconfigure_runtime("base")
Z_diamonds <- CECsample_diamonds(n_max = 80, seed = 1)
str(Z_diamonds)

diag_diamonds <- CECfitLambdaGrid(
  Z = Z_diamonds,
  lambda_grid = seq(0.2, 0.8, by = 0.2),
  k0 = 2,
  B = 2,
  C = 10,
  r0 = 5,
  Nshots_fresh = 1,
  Nshots_warm = 1,
  Nloop = 10,
  familyType = "gaussAndDiscreteVector",
  seed = 1,
  silent = TRUE,
  verbose = FALSE,
  n_cores = 1L,
  checkpoint_dir = FALSE,
  auto_checkpoint = FALSE,
  show_progress = FALSE
)
head(diag_diamonds$summary)
\dontshow{\}) # examplesIf}
}
