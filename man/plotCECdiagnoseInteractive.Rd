% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ceclust.R
\name{plotCECdiagnoseLambdaGrid}
\alias{plotCECdiagnoseLambdaGrid}
\alias{plotCECBestPartitions1D}
\alias{build_partition_path_df}
\alias{plot_partition_path_pca}
\alias{plotCECdiagnoseInteractive}
\title{Plot diagnostic summaries and interactive views for CEClust workflows.}
\usage{
plotCECdiagnoseLambdaGrid(
  lambda_diag,
  ratio_col = "stab_ratio_mean",
  stab0_col = "stab0_mean",
  stabB_col = "stabB_mean",
  H_col = "meanH_B",
  use_smoothed = TRUE,
  lambda_min_obj = NULL,
  lambda_stable_obj = NULL,
  unstable_col = grDevices::adjustcolor("grey70", alpha.f = 0.35),
  saturation_mark = c("hatch", "shade", "none"),
  saturation_col = grDevices::adjustcolor("grey15", alpha.f = 0.6),
  saturation_fill_col = grDevices::adjustcolor("white", alpha.f = 0.15),
  saturation_border_col = grDevices::adjustcolor("grey10", alpha.f = 0.75),
  saturation_border_lwd = 0.8,
  saturation_density = 12,
  saturation_angle = -45
)

plotCECBestPartitions1D(
  Z,
  best_partitions_obj,
  lambda_subset = NULL,
  max_plots = 6,
  digits_lambda = 3,
  digits_H = 4
)

build_partition_path_df(Z, best_parts, scale_data = TRUE, lambda_subset = NULL)

plot_partition_path_pca(path_df)

plotCECdiagnoseInteractive(
  obj,
  Z,
  source = c("fixed", "bootstrap", "all"),
  criterion = c("projected_H", "raw_H"),
  launch.browser = TRUE,
  scale_pca = TRUE
)
}
\arguments{
\item{lambda_diag}{Object returned by \code{\link[=CECdiagnose_lambda_grid_linked]{CECdiagnose_lambda_grid_linked()}}.}

\item{ratio_col, stab0_col, stabB_col, H_col}{Column names used in the lambda
summary plot.}

\item{use_smoothed}{Logical. If \code{TRUE}, smoothed summary columns are used when
available.}

\item{lambda_min_obj}{Deprecated compatibility object containing
\code{lambda_min}.}

\item{lambda_stable_obj}{Optional output of \code{\link[=CECidentifyStableLambdas]{CECidentifyStableLambdas()}} used
to shade stable and unstable regions. When the stable-object carries
saturation diagnostics, lambdas excluded by saturation can be highlighted
separately.}

\item{unstable_col}{Colour used to shade unstable lambda bands.}

\item{saturation_mark}{Visual mark used for lambdas excluded by the
saturation filter only. \code{"hatch"} uses reverse diagonal hatching, \code{"shade"}
uses a semi-transparent overlay, and \code{"none"} disables the extra layer.}

\item{saturation_col, saturation_fill_col, saturation_border_col, saturation_border_lwd, saturation_density, saturation_angle}{Graphical
parameters used when drawing saturation-only exclusions.}

\item{Z}{Original data set used to build the diagnostic object.}

\item{best_partitions_obj}{Object returned by \code{\link[=CECextractBestPartitions]{CECextractBestPartitions()}}.}

\item{lambda_subset}{Optional subset of lambda values to display.}

\item{max_plots}{Maximum number of panels shown in the histogram overview.}

\item{digits_lambda, digits_H}{Number of digits used in panel annotations.}

\item{best_parts}{Object returned by \code{\link[=CECextractBestPartitions]{CECextractBestPartitions()}}.}

\item{scale_data}{Logical. If \code{TRUE}, numeric variables are scaled before PCA.}

\item{path_df}{Data frame returned by \code{\link[=build_partition_path_df]{build_partition_path_df()}}.}

\item{obj}{Either a \code{lambda_diag} object returned by
\code{\link[=CECdiagnose_lambda_grid_linked]{CECdiagnose_lambda_grid_linked()}} or a \code{best_parts} object returned by
\code{\link[=CECextractBestPartitions]{CECextractBestPartitions()}}.}

\item{source}{Candidate pool used when \code{obj} is a \code{lambda_diag} object and
best partitions still need to be extracted.}

\item{criterion}{Criterion used when extracting best partitions from a
\code{lambda_diag} object.}

\item{launch.browser}{Logical passed to \code{shiny::runApp()}.}

\item{scale_pca}{Logical. If \code{TRUE}, numeric variables are scaled before PCA
is computed inside the interactive application.}
}
\value{
\code{plotCECdiagnoseLambdaGrid()} is called for its side effects and
returns \code{NULL} invisibly.

\code{plotCECBestPartitions1D()} is called for its side effects and returns
\code{NULL} invisibly.

\code{build_partition_path_df()} returns a long data frame with PCA
coordinates, lambda values, cluster labels, and optional species labels for
faceted plotting across lambdas.

\code{plot_partition_path_pca()} returns a \code{ggplot2} object.

\code{plotCECdiagnoseInteractive()} launches a local Shiny application and
returns the value produced by \code{shiny::runApp()}.
}
\description{
The plotting helpers provide complementary views of a CEClust analysis:
static summaries of the lambda grid, one-dimensional inspection of extracted
partitions, PCA-based views of partition trajectories, and an interactive
Shiny explorer for mixed-data results.
}
\details{
\code{plotCECdiagnoseInteractive()} requires the optional packages
\pkg{shiny} and \pkg{ggplot2}. For one-dimensional numeric data, the app
switches automatically to a histogram-based display.
}
\examples{
Z <- simulate_multidim_benchmark_data(n = 80, p_num = 3, p_fac = 2, seed = 3)
lambda_diag <- CECdiagnose_lambda_grid_linked(
  Z = Z,
  lambda_grid = seq(0.2, 0.8, by = 0.2),
  k0 = 2,
  B = 2,
  C = 10,
  r0 = 6,
  Nshots_fresh = 1,
  Nshots_warm = 1,
  Nloop = 12,
  familyType = "gaussAndDiscreteVector",
  seed = 3,
  silent = TRUE,
  verbose = FALSE,
  n_cores = 1L,
  checkpoint_dir = FALSE,
  auto_checkpoint = FALSE,
  show_progress = FALSE
)
best_parts <- CECextractBestPartitions(lambda_diag, source = "fixed", Z = Z)

plotCECdiagnoseLambdaGrid(lambda_diag, use_smoothed = FALSE)

path_df <- build_partition_path_df(iris[, -5], CECextractBestPartitions(
  CECdiagnose_lambda_grid_linked(
    iris[, -5],
    lambda_grid = seq(0.2, 0.4, by = 0.2),
    k0 = 2,
    B = 1,
    C = 10,
    r0 = 4,
    Nshots_fresh = 1,
    Nshots_warm = 1,
    Nloop = 10,
    familyType = "gaussVector",
    seed = 1,
    silent = TRUE,
    verbose = FALSE,
    n_cores = 1L,
    checkpoint_dir = FALSE,
    auto_checkpoint = FALSE,
    show_progress = FALSE
  ),
  source = "fixed",
  Z = iris[, -5]
))
plot_partition_path_pca(path_df)

\dontshow{if (interactive() && requireNamespace("shiny", quietly = TRUE) && requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
plotCECdiagnoseInteractive(best_parts, Z, launch.browser = FALSE)
\dontshow{\}) # examplesIf}
}
