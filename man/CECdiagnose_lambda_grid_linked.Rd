% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ceclust.R
\name{CECaddSmoothedDiagnostics}
\alias{CECaddSmoothedDiagnostics}
\alias{CECidentifyStableLambdas}
\alias{CECdiagnose_lambda_grid_linked}
\title{Add smoothed summary curves to a linked-lambda diagnostic object.}
\usage{
CECaddSmoothedDiagnostics(
  lambda_diag,
  k = 3,
  ratio_col = "stab_ratio_mean",
  stab0_col = "stab0_mean",
  stabB_col = "stabB_mean",
  H_col = "meanH_B"
)

CECidentifyStableLambdas(
  lambda_diag,
  rule = c("ratio", "stabB", "both"),
  ratio_threshold = 0.8,
  stabB_threshold = 0.8,
  use_smoothed = TRUE,
  min_consecutive = 1,
  ratio_col = "stab_ratio_mean",
  stabB_col = "stabB_mean"
)

CECdiagnose_lambda_grid_linked(
  Z,
  lambda_grid = seq(0.1, 2, by = 0.1),
  k0 = 10,
  B = 10,
  C = 1,
  r0 = NULL,
  Nshots_fresh = 5,
  Nshots_warm = 5,
  Nshots_init = NULL,
  Nloop = 300,
  familyType = "gaussAndDiscreteVector",
  sizeMaxOutlier = 0,
  autoRegroupOutliers = FALSE,
  focus = NULL,
  replace = TRUE,
  seed = NULL,
  silent = TRUE,
  verbose = TRUE,
  n_cores = 1L,
  checkpoint_dir = NULL,
  auto_checkpoint = NULL,
  resume = TRUE,
  batch_size = NULL,
  show_progress = NULL,
  rerun_failed_serial = TRUE,
  checkpoint_compress = FALSE,
  partition_metric = c("match", "ARI"),
  stability_approach = c("pairwise", "reference_bestH")
)
}
\arguments{
\item{lambda_diag}{Object returned by \code{\link[=CECdiagnose_lambda_grid_linked]{CECdiagnose_lambda_grid_linked()}}.}

\item{k}{Width of the moving-average smoother applied to the selected summary
columns.}

\item{ratio_col, stab0_col, stabB_col, H_col}{Column names in
\code{lambda_diag$summary} used for smoothing.}

\item{rule}{Stability rule. \code{"ratio"} uses the bootstrap-to-fixed stability
ratio, \code{"stabB"} uses bootstrap stability alone, and \code{"both"} requires the
two conditions simultaneously.}

\item{ratio_threshold, stabB_threshold}{Thresholds used by the selected
stability rule.}

\item{use_smoothed}{Logical. If \code{TRUE}, the smoothed columns generated by
\code{\link[=CECaddSmoothedDiagnostics]{CECaddSmoothedDiagnostics()}} are used when available.}

\item{min_consecutive}{Minimum run length required for a lambda value to be
declared part of a stable region.}

\item{Z}{Input data used for all fits and projections.}

\item{lambda_grid}{Numeric vector of candidate \code{lambda} values. Duplicates are
removed and the grid is sorted increasingly.}

\item{k0}{Number of linked paths fitted on the original data.}

\item{B}{Number of linked bootstrap paths.}

\item{C}{Positive tuning constant used by the composite entropy criterion.}

\item{r0}{Optional upper bound for the initial number of clusters.}

\item{Nshots_fresh}{Number of fresh random starts used at the first lambda of
each path.}

\item{Nshots_warm}{Number of warm starts used for neighbouring lambda values
along a path.}

\item{Nshots_init}{Deprecated compatibility alias that can still initialise
\code{Nshots_fresh} and \code{Nshots_warm}.}

\item{Nloop}{Maximum number of optimisation iterations per fit.}

\item{familyType}{Model family passed to \code{\link[=CECclassif]{CECclassif()}}.}

\item{sizeMaxOutlier, autoRegroupOutliers, focus}{Additional controls forwarded
to \code{\link[=CECclassif]{CECclassif()}}.}

\item{replace}{Logical. If \code{TRUE}, bootstrap samples are drawn with
replacement.}

\item{seed}{Optional random seed used for reproducibility.}

\item{silent, verbose}{Logical controls for fit-level and workflow-level
messaging.}

\item{n_cores}{Number of worker processes used for independent path tasks.}

\item{checkpoint_dir}{Directory used to store per-task \code{.rds} checkpoints.
Use \code{TRUE} to request an automatically generated temporary folder and
\code{FALSE} to disable checkpointing explicitly.}

\item{auto_checkpoint}{Logical. If \code{NULL}, checkpointing is enabled
automatically when \code{n_cores > 1}.}

\item{resume}{Logical. If \code{TRUE}, completed tasks already present in
\code{checkpoint_dir} are reused.}

\item{batch_size}{Number of tasks launched together in each parallel batch.}

\item{show_progress}{Logical. If \code{TRUE}, progress lines are printed while
tasks complete.}

\item{rerun_failed_serial}{Logical. If \code{TRUE}, unfinished tasks are retried
sequentially after a parallel batch failure.}

\item{checkpoint_compress}{Logical passed to \code{saveRDS()} for checkpoint
files.}

\item{partition_metric}{Similarity measure used for partition comparisons.
\code{"match"} relies on an optimal label matching; \code{"ARI"} uses the adjusted
Rand index.}

\item{stability_approach}{Strategy used to summarise bootstrap stability.}
}
\value{
\code{CECaddSmoothedDiagnostics()} returns the input \code{lambda_diag} object
with additional \verb{*_smooth} columns in \code{summary} and a \code{smoothing}
component describing the chosen settings.

\code{CECidentifyStableLambdas()} returns a list with the first stable
lambda (\code{lambda_min}), the complete stable mask, detected stable runs, and
a compact summary data frame.

\code{CECdiagnose_lambda_grid_linked()} returns a list with:
\itemize{
\item \code{summary}: one row per lambda with stability and criterion summaries;
\item \code{details}: the full per-lambda objects used to build downstream summaries;
\item \code{lambda_grid}: the analysed lambda grid;
\item \code{partition_metric} and \code{stability_approach}: the comparison settings used;
\item \code{linked}: a compact description of the linked fixed and bootstrap paths;
\item \code{execution}: runtime metadata, including checkpoint information.
}
}
\description{
\code{CECdiagnose_lambda_grid_linked()} is the main workflow for studying how the
fitted partition evolves across a sequence of regularisation values. For each
\code{lambda}, the function combines repeated runs on the original data with
bootstrap trajectories projected back on the reference data set. The result
can be checkpointed and resumed, which is helpful for long multi-core runs.
}
\examples{
Z <- simulate_multidim_benchmark_data(n = 80, p_num = 3, p_fac = 2, seed = 1)
runtime <- CECconfigure_runtime("base")

lambda_diag <- CECdiagnose_lambda_grid_linked(
  Z = Z,
  lambda_grid = seq(0.2, 0.8, by = 0.2),
  k0 = 2,
  B = 2,
  C = 10,
  r0 = 6,
  Nshots_fresh = 1,
  Nshots_warm = 1,
  Nloop = 12,
  familyType = "gaussAndDiscreteVector",
  seed = 1,
  silent = TRUE,
  verbose = FALSE,
  n_cores = runtime$n_cores,
  checkpoint_dir = FALSE,
  auto_checkpoint = FALSE,
  show_progress = FALSE
)

lambda_diag <- CECaddSmoothedDiagnostics(lambda_diag, k = 3)
stable <- CECidentifyStableLambdas(lambda_diag, rule = "ratio")

head(lambda_diag$summary)
stable$lambda_min
}
