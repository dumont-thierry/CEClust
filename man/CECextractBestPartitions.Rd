% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ceclust.R
\name{CECdetectPartitionChanges}
\alias{CECdetectPartitionChanges}
\alias{CECcheckBestPartitionCoherence}
\alias{CECrepairBestPartitionTrajectory}
\alias{CECextractBestPartitions}
\title{Detect change points along a best-partition trajectory.}
\usage{
CECdetectPartitionChanges(
  best_partitions_obj,
  partition_metric = c("match", "ARI"),
  sim_threshold = 0.8,
  criterion = c("REO_or_threshold", "REO_only", "threshold_only", "REO_and_threshold"),
  include_first = TRUE,
  method = c("consecutive", "regime_reference"),
  lambda_subset = NULL
)

CECcheckBestPartitionCoherence(best_partitions_obj, Z, tol = 1e-10)

CECrepairBestPartitionTrajectory(
  best_partitions_obj,
  Z,
  tol = 1e-10,
  max_iter = 100
)

CECextractBestPartitions(
  lambda_diag,
  source = c("all", "fixed", "bootstrap"),
  criterion = c("projected_H", "raw_H"),
  Z = NULL
)
}
\arguments{
\item{best_partitions_obj}{Object returned by \code{\link[=CECextractBestPartitions]{CECextractBestPartitions()}}.}

\item{partition_metric}{Similarity measure used to compare consecutive
partitions.}

\item{sim_threshold}{Threshold below which a similarity-based change is
detected.}

\item{criterion}{Rule used to declare a change. In
\code{CECextractBestPartitions()} this argument selects the criterion used to
choose the representative partition at each lambda. In
\code{CECdetectPartitionChanges()} it selects the rule combining REO changes and
partition similarity.}

\item{include_first}{Logical. If \code{TRUE}, the first lambda value is always
reported as the start of a new regime.}

\item{method}{Change-detection method. \code{"consecutive"} compares each
partition to the previous lambda. \code{"regime_reference"} compares each
partition to the reference partition at the beginning of the current
regime and updates the reference when a change is detected.}

\item{lambda_subset}{Optional subset of lambda values on which changes are
detected. This is useful for detecting regimes only on stable lambdas.}

\item{Z}{Original data set. It is required for coherence checks and repair
operations, and recommended when extracting partitions so that raw criterion
decompositions can be recovered when available.}

\item{tol}{Numerical tolerance used when comparing criterion values.}

\item{max_iter}{Maximum number of forward/backward propagation rounds used by
the repair heuristic.}

\item{lambda_diag}{Object returned by \code{\link[=CECdiagnose_lambda_grid_linked]{CECdiagnose_lambda_grid_linked()}}.}

\item{source}{Candidate pool used when selecting the representative partition:
\code{"fixed"} uses runs on the original data, \code{"bootstrap"} uses projected
bootstrap fits, and \code{"all"} combines both.}
}
\value{
\code{CECdetectPartitionChanges()} returns a list with a summary data
frame, the lambda values flagged as change points, and the settings used to
detect them.

\code{CECcheckBestPartitionCoherence()} returns a list with a row per
lambda, a global coherence flag, and the indices/lambda values of
incoherent positions.

\code{CECrepairBestPartitionTrajectory()} returns an updated
\code{best_partitions_obj} enriched with \code{repair_log}, coherence summaries
before/after repair, and convergence information.

\code{CECextractBestPartitions()} returns a list with:
\itemize{
\item \code{best}: a list of per-lambda representative partitions;
\item \code{summary}: a compact data frame with criterion values, REO, and origin
labels;
\item \code{source} and \code{criterion}: the selection settings used.
}
}
\description{
\code{CECextractBestPartitions()} condenses the output of
\code{\link[=CECdiagnose_lambda_grid_linked]{CECdiagnose_lambda_grid_linked()}} into one representative partition per
lambda value. Downstream helpers can then assess coherence, repair
trajectories, and identify change points along the selected lambda path.
}
\examples{
CECconfigure_runtime("base")
Z <- simulate_multidim_benchmark_data(n = 80, p_num = 3, p_fac = 2, seed = 2)
lambda_diag <- CECdiagnose_lambda_grid_linked(
  Z = Z,
  lambda_grid = seq(0.2, 0.8, by = 0.2),
  k0 = 2,
  B = 2,
  C = 10,
  r0 = 6,
  Nshots_fresh = 1,
  Nshots_warm = 1,
  Nloop = 12,
  familyType = "gaussAndDiscreteVector",
  seed = 2,
  silent = TRUE,
  verbose = FALSE,
  n_cores = 1L,
  checkpoint_dir = FALSE,
  auto_checkpoint = FALSE,
  show_progress = FALSE
)

best_parts <- CECextractBestPartitions(
  lambda_diag,
  source = "fixed",
  criterion = "projected_H",
  Z = Z
)
coherence <- CECcheckBestPartitionCoherence(best_parts, Z)
changes <- CECdetectPartitionChanges(best_parts)

head(best_parts$summary)
coherence$all_coherent
changes$change_lambdas
}
