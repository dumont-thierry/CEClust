% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ceclust.R, R/runtime_helpers.R
\name{CECreset_fast_backend}
\alias{CECreset_fast_backend}
\alias{CECset_fast_backend}
\alias{CECperformanceInfo}
\alias{CECconfigure_runtime}
\title{Reset cached information about the optional fast backend.}
\usage{
CECreset_fast_backend(rebuild = FALSE)

CECset_fast_backend(enabled = TRUE, rebuild = enabled)

CECperformanceInfo()

CECconfigure_runtime(
  profile = c("auto", "base", "fast"),
  n_cores = NULL,
  max_cores = 8L,
  prefer_multicore = TRUE,
  rebuild_cpp = FALSE
)
}
\arguments{
\item{rebuild}{Logical. If \code{TRUE}, refresh backend detection immediately
after toggling the backend state.}

\item{enabled}{Logical. If \code{TRUE}, CEClust is allowed to use the compiled
backend when it is available.}

\item{profile}{One of \code{"auto"}, \code{"base"}, or \code{"fast"}.}

\item{n_cores}{Optional manual number of cores. If \code{NULL}, a conservative
automatic choice is made.}

\item{max_cores}{Upper bound used when \code{n_cores} is not supplied.}

\item{prefer_multicore}{Logical. If \code{FALSE}, the returned configuration uses
one core even when more are available.}

\item{rebuild_cpp}{Logical. Kept for compatibility. If \code{TRUE}, the function
refreshes the detection of the compiled backend that was built when the
package was installed.}
}
\value{
\code{CECreset_fast_backend()} invisibly returns \code{TRUE}.

\code{CECset_fast_backend()} invisibly returns the named list produced by
\code{\link[=CECperformanceInfo]{CECperformanceInfo()}}.

\code{CECperformanceInfo()} returns a named list containing
\code{fast_backend_available}, \code{fast_backend_forced_disabled},
\code{make_available}, \code{BLAS}, \code{LAPACK}, and \code{detected_cores}.

A list with class \code{"CEC_runtime_config"} containing:
\itemize{
\item \code{profile}: the selected execution profile;
\item \code{use_cpp}: whether the optional compiled backend is active;
\item \code{n_cores}: the recommended number of workers for
\code{\link[=CECdiagnose_lambda_grid_linked]{CECdiagnose_lambda_grid_linked()}};
\item \code{max_cores} and \code{prefer_multicore}: the decision inputs used by the helper;
\item \code{performance}: the output of \code{\link[=CECperformanceInfo]{CECperformanceInfo()}}.
}
}
\description{
This helper provides an easy entry point for users who want either a
conservative pure-R setup or an automatic "use the fast path when available"
setup. The function immediately applies the backend choice through
\code{\link[=CECset_fast_backend]{CECset_fast_backend()}} and returns the number of cores that should be passed
to \code{\link[=CECdiagnose_lambda_grid_linked]{CECdiagnose_lambda_grid_linked()}}.
}
\details{
The \code{"base"} profile disables the optional compiled backend and forces
single-core execution. The \code{"auto"} profile activates the compiled backend
when available and chooses a conservative number of worker processes. The
\code{"fast"} profile requests the same backend explicitly and warns when the
compiled code is unavailable on the current machine.
}
\seealso{
\code{\link[=CECperformanceInfo]{CECperformanceInfo()}}, \code{\link[=CECset_fast_backend]{CECset_fast_backend()}},
\code{\link[=CECreset_fast_backend]{CECreset_fast_backend()}}
}
